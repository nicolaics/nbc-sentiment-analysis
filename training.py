from preprocess import preprocess

def accuracy():
    # (true negative + true positive) / 
    # (true positive + false positive + true negative + false negative)
    pass


word_features = preprocess()

